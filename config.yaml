name: "Website"
app_id: "yeti-www"
customer_id: "yeti"
version: "1.0.0"
description: "yetirocks.com marketing site + demos"
route_prefix: "/"

schemas:
  path: schemas/schema.graphql

static:
  path: web
  route: /
  spa: true
  build:
    source: source
    command: npm run build
